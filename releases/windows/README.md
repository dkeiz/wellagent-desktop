# Windows Portable Builds

Portable Windows builds often exceed GitHub's `100 MB` per-file Git limit.

Use GitHub Release assets for portable desktop binaries. If you later decide to version them directly, do it with Git LFS and a deliberate policy instead of regular Git blobs.
