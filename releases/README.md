# Release Artifacts

This directory holds intentionally published release assets that are meant to stay alongside the open-source source tree.

Current policy:

- `android/` may track the current installable APK and its matching metadata file.
- `windows/` documents desktop portable distribution policy.
- Older archives stay in local build folders or GitHub Release assets, not in regular source history.
