# Release Prep - Work Completed (2026-05-28)

Target release: `0.1.0-beta.1`.

## Beta Readiness Fixes

- Aligned the Pixel Avatar UX contract with the preset-based implementation.
- Split `agentin/plugins/pixel-avatar/avatar.js` into shared helpers plus per-character renderer files, all below the 1000-line source budget.
- Removed the temporary Pixel Avatar line-budget allowlist.
- Kept the staged removal of tracked A2A task/event logs and Python bytecode.
- Removed tracked timestamped sub-agent verifier folders from the previous live-test jobs.
- Added ignore rules for A2A runtime data, timestamped sub-agent verifier folders, and Python bytecode in `.gitignore` and `.dockerignore`.
- Added a release-runtime-artifacts contract so tracked/runtime packaged paths fail the contract gate.
- Replaced raw `agentin/**` desktop packaging with an explicit `extraFiles` include/exclude filter for curated defaults.
- Moved Remote Gateway shared secret persistence to credential storage and added migration from legacy plaintext and generic migrated credential keys.
- Updated Companion Browser pairing contract expectations to match `client.createChatSession()` through `ensureActiveSession()`.

## Direct Fixes

- Fixed `PortListenerManager` CORS origin handling and request timeout cleanup.
- Fixed `addPromptRule()` return shape so DB defaults are reflected.
- Added database and folder cleanup for `deleteAgent()`.
- Removed dead helpers and redundant requires.
- Removed noisy debug logging from model/tool IPC paths.

## Release Metadata

- Bumped root, `packages/wellbot`, and mobile metadata to `0.1.0-beta.1`.
- Updated README alpha/beta/security/npm wording.
- Added `repository` and `homepage` metadata to `packages/wellbot/package.json`.
- Updated `packages/wellbot/README.md` for the beta CLI release.
- Added `docs/architectural-debt.md` for larger follow-up work outside this release gate.

## Verification Gates

Bounded verification completed:

- `node tests/run-suite.js contracts` - PASS, 126 contract modules.
- `npm run pack:wellbot` - PASS, 4-file dry-run tarball.
- `node tests/run-suite.js quick` - PASS, 131 items after rerunning outside the sandbox because the sandbox returned `spawn EPERM`.
- `node tests/run-suite.js core` - PASS, 132 items outside the sandbox.

Wellbot tarball contents stayed limited to:

- `LICENSE`
- `README.md`
- `bin/wellbot.js`
- `package.json`
